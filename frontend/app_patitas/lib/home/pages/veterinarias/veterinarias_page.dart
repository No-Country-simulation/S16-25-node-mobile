import 'package:flutter/widgets.dart';

class VeterinariasPage extends StatelessWidget {
  const VeterinariasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
